(model.Task.methods.slowdown = function () {				//Used in Synchronous vs. Asynchronous example.				var i, j, len1 = 10000, len2 = 7000;				for (i = 0; i < len1; i++) {					for (j = 0; j < len2; j++) {}				}								return "Slow down, you move to fast...";			}).scope = "public";(model.Task.methods.createNewTask = function () {				// create new Task entity				return new ds.Task();			}).scope = "public";